/* ===== Design Tokens ===== */
:root,
[data-theme='light'] {
  /* Warm food-inspired palette */
  --color-bg: #faf8f4;
  --color-surface: #fffefb;
  --color-surface-2: #f5f2ec;
  --color-surface-offset: #efebe3;
  --color-border: #e0dbd0;
  --color-divider: #eae5dc;

  --color-text: #2a2520;
  --color-text-muted: #7a716a;
  --color-text-faint: #b5ada4;
  --color-text-inverse: #fffefb;

  --color-primary: #c4571e;
  --color-primary-hover: #a8461a;
  --color-primary-active: #8a3a16;
  --color-primary-highlight: #fce8d8;

  --color-teal: #0d7377;
  --color-teal-hover: #0a5d60;

  --color-success: #4a7c2e;
  --color-success-bg: #e8f0e0;
  --color-warning: #b8860b;
  --color-warning-bg: #fdf5e0;
  --color-error: #c0392b;
  --color-error-bg: #fce8e5;

  /* Spacing */
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-5: 1.25rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-10: 2.5rem;
  --space-12: 3rem;

  /* Radius */
  --radius-sm: 0.375rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;
  --radius-xl: 1rem;

  /* Fonts */
  --font-display: 'Fraunces', Georgia, serif;
  --font-body: 'DM Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

  /* Shadows */
  --shadow-sm: 0 1px 3px rgba(42, 37, 32, 0.06);
  --shadow-md: 0 4px 12px rgba(42, 37, 32, 0.08);
  --shadow-lg: 0 12px 32px rgba(42, 37, 32, 0.12);

  --transition: 180ms cubic-bezier(0.16, 1, 0.3, 1);

  --content-width: 1100px;
}

[data-theme='dark'] {
  --color-bg: #1a1816;
  --color-surface: #221f1c;
  --color-surface-2: #2a2622;
  --color-surface-offset: #2e2a26;
  --color-border: #3d3833;
  --color-divider: #34302c;

  --color-text: #e8e2db;
  --color-text-muted: #9a9088;
  --color-text-faint: #6b635c;
  --color-text-inverse: #1a1816;

  --color-primary: #e8783a;
  --color-primary-hover: #f08a4c;
  --color-primary-active: #d66828;
  --color-primary-highlight: #3a2820;

  --color-teal: #4fb0b4;
  --color-teal-hover: #6bc4c8;

  --color-success: #7bc04a;
  --color-success-bg: #2a3320;
  --color-warning: #e0a830;
  --color-warning-bg: #3a2e18;
  --color-error: #e85d4a;
  --color-error-bg: #3a201c;

  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 12px 32px rgba(0, 0, 0, 0.5);
}

/* ===== Base ===== */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
  scroll-behavior: smooth;
}

body {
  min-height: 100dvh;
  line-height: 1.6;
  font-family: var(--font-body);
  font-size: 15px;
  color: var(--color-text);
  background-color: var(--color-bg);
}

a { color: var(--color-primary); text-decoration: none; transition: color var(--transition); }
a:hover { color: var(--color-primary-hover); }

button { cursor: pointer; font: inherit; color: inherit; background: none; border: none; }

input, select, textarea {
  font: inherit; color: inherit;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: var(--space-1) var(--space-2);
  transition: border-color var(--transition), box-shadow var(--transition);
}
input:focus, select:focus, textarea:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-highlight);
}

::selection { background: var(--color-primary); color: var(--color-text-inverse); }

:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; border-radius: var(--radius-sm); }

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* ===== Layout ===== */
.container {
  max-width: var(--content-width);
  margin: 0 auto;
  padding: var(--space-6) var(--space-4);
  display: flex;
  flex-direction: column;
  gap: var(--space-6);
}

/* ===== Header ===== */
.app-header {
  background: var(--color-surface);
  border-bottom: 1px solid var(--color-border);
  box-shadow: var(--shadow-sm);
  position: sticky;
  top: 0;
  z-index: 100;
}

.header-inner {
  max-width: var(--content-width);
  margin: 0 auto;
  padding: var(--space-3) var(--space-4);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.brand { display: flex; align-items: center; gap: var(--space-3); }

.brand-logo {
  width: 32px; height: 32px;
  color: var(--color-primary);
  flex-shrink: 0;
}

.brand-title {
  font-family: var(--font-display);
  font-size: 1.25rem;
  font-weight: 700;
  line-height: 1.2;
  color: var(--color-text);
}

.brand-sub {
  font-size: 0.75rem;
  color: var(--color-text-muted);
  line-height: 1.2;
}

.theme-toggle {
  width: 36px; height: 36px;
  display: flex; align-items: center; justify-content: center;
  border-radius: var(--radius-md);
  color: var(--color-text-muted);
  transition: background var(--transition), color var(--transition);
}
.theme-toggle:hover { background: var(--color-surface-2); color: var(--color-text); }

/* ===== Tabs ===== */
.tabs {
  max-width: var(--content-width);
  margin: 0 auto;
  padding: 0 var(--space-4);
  display: flex;
  gap: var(--space-1);
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: thin;
}

.tab {
  padding: var(--space-2) var(--space-4);
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--color-text-muted);
  border-radius: var(--radius-md) var(--radius-md) 0 0;
  border-bottom: 2px solid transparent;
  white-space: nowrap;
  transition: color var(--transition), border-color var(--transition);
}
.tab:hover { color: var(--color-text); }
.tab.active {
  color: var(--color-primary);
  border-bottom-color: var(--color-primary);
  font-weight: 700;
}

/* ===== Tab content ===== */
.tab-content { display: none; }
.tab-content.active { display: block; animation: fadeIn 200ms ease; }

@keyframes fadeIn { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }

/* ===== Cards ===== */
.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  box-shadow: var(--shadow-sm);
}

.card-header-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: var(--space-3);
  margin-bottom: var(--space-3);
}

.card-title {
  font-family: var(--font-display);
  font-size: 1.125rem;
  font-weight: 700;
  margin-bottom: var(--space-1);
  color: var(--color-text);
}

.card-hint {
  font-size: 0.8125rem;
  color: var(--color-text-muted);
  margin-bottom: var(--space-4);
}

/* ===== Tables ===== */
.table-wrap {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
}

.data-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.8125rem;
}

.data-table th {
  background: var(--color-surface-2);
  font-weight: 700;
  text-align: left;
  padding: var(--space-2) var(--space-3);
  border-bottom: 1px solid var(--color-border);
  white-space: nowrap;
  color: var(--color-text);
}

.data-table th.num, .data-table td.num { text-align: right; font-variant-numeric: tabular-nums; }

.data-table td {
  padding: var(--space-2) var(--space-3);
  border-bottom: 1px solid var(--color-divider);
  vertical-align: middle;
}

.data-table tbody tr:hover { background: var(--color-surface-2); }

.data-table tfoot td {
  font-weight: 700;
  background: var(--color-surface-2);
  border-bottom: none;
  border-top: 2px solid var(--color-border);
}

.total-row td { color: var(--color-text); }

/* Inline editable cells */
.data-table input, .data-table select {
  min-width: 70px;
  border: 1px solid transparent;
  background: transparent;
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
  font-size: 0.8125rem;
}
.data-table input[type="text"] { min-width: 140px; }
.data-table input[type="number"] { min-width: 55px; text-align: right; font-variant-numeric: tabular-nums; }
.data-table input:hover, .data-table select:hover { border-color: var(--color-border); }
.data-table input:focus, .data-table select:focus { border-color: var(--color-primary); box-shadow: none; }

/* Delete button in table */
.row-delete {
  width: 24px; height: 24px;
  display: inline-flex; align-items: center; justify-content: center;
  color: var(--color-text-faint);
  border-radius: var(--radius-sm);
  transition: color var(--transition), background var(--transition);
}
.row-delete:hover { color: var(--color-error); background: var(--color-error-bg); }

/* ===== Buttons ===== */
.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-4);
  font-size: 0.875rem;
  font-weight: 500;
  border-radius: var(--radius-md);
  background: var(--color-primary);
  color: var(--color-text-inverse);
  border: 1px solid transparent;
  transition: background var(--transition), transform var(--transition);
}
.btn:hover { background: var(--color-primary-hover); }
.btn:active { transform: scale(0.98); }

.btn-sm { padding: var(--space-1) var(--space-3); font-size: 0.8125rem; }

.btn-outline {
  background: transparent;
  color: var(--color-primary);
  border-color: var(--color-border);
}
.btn-outline:hover { background: var(--color-primary-highlight); border-color: var(--color-primary); }

.btn-danger { background: var(--color-error); }
.btn-danger:hover { background: #a33222; }

.journal-actions { display: flex; gap: var(--space-2); flex-wrap: wrap; }

/* ===== Stats grid ===== */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: var(--space-3);
}

.stat-item {
  background: var(--color-surface-2);
  border-radius: var(--radius-md);
  padding: var(--space-3) var(--space-4);
  border: 1px solid var(--color-border);
}

.stat-label {
  font-size: 0.75rem;
  color: var(--color-text-muted);
  margin-bottom: var(--space-1);
  text-transform: uppercase;
  letter-spacing: 0.03em;
}

.stat-value {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--color-text);
  font-variant-numeric: tabular-nums;
}

.stat-note {
  font-size: 0.6875rem;
  color: var(--color-text-faint);
  margin-top: var(--space-1);
}

/* ===== Eval grid ===== */
.eval-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}

.eval-item {
  background: var(--color-surface-2);
  border-radius: var(--radius-md);
  padding: var(--space-3) var(--space-4);
  border-left: 3px solid var(--color-border);
}

.eval-item.good { border-left-color: var(--color-success); }
.eval-item.warn { border-left-color: var(--color-warning); }
.eval-item.bad { border-left-color: var(--color-error); }

.eval-label {
  font-size: 0.75rem;
  color: var(--color-text-muted);
  margin-bottom: var(--space-1);
  font-weight: 500;
}

.eval-value { font-size: 0.875rem; color: var(--color-text); }

/* Mode recommendation */
.mode-recommendation {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  padding: var(--space-4);
  background: var(--color-teal);
  color: #fff;
  border-radius: var(--radius-md);
  margin-bottom: var(--space-3);
}

.mode-rec-label {
  font-size: 0.8125rem;
  opacity: 0.85;
}

.mode-rec-value {
  font-family: var(--font-display);
  font-size: 1.125rem;
  font-weight: 700;
}

.mode-select {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.mode-select label {
  font-size: 0.8125rem;
  color: var(--color-text-muted);
  font-weight: 500;
}

.select-input {
  min-width: 160px;
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
  background: var(--color-surface);
}

/* ===== Tips list ===== */
.tips-list { list-style: none; display: flex; flex-direction: column; gap: var(--space-2); }

.tips-list li {
  padding: var(--space-2) var(--space-3);
  background: var(--color-surface-2);
  border-radius: var(--radius-sm);
  font-size: 0.8125rem;
  line-height: 1.5;
  border-left: 3px solid var(--color-primary);
}

.tips-list li:nth-child(even) { background: var(--color-surface-offset); }

/* ===== Steps list ===== */
.steps-list { list-style: none; counter-reset: step; display: flex; flex-direction: column; gap: var(--space-3); }

.steps-list li {
  counter-increment: step;
  display: flex;
  gap: var(--space-3);
  padding: var(--space-3);
  background: var(--color-surface-2);
  border-radius: var(--radius-md);
}

.steps-list li::before {
  content: counter(step);
  flex-shrink: 0;
  width: 28px; height: 28px;
  display: flex; align-items: center; justify-content: center;
  background: var(--color-primary);
  color: var(--color-text-inverse);
  border-radius: 50%;
  font-size: 0.8125rem;
  font-weight: 700;
}

.steps-list .step-title { font-weight: 700; font-size: 0.875rem; }
.steps-list .step-desc { font-size: 0.8125rem; color: var(--color-text-muted); margin-top: 2px; }

/* Ensure wide tables scroll horizontally without clipping */
.table-wrap { overflow-x: auto; }
.data-table { min-width: 500px; }
.journal-table { min-width: 1600px; }
#ingredients-db-table { min-width: 1100px; }
.score-cell { text-align: center; font-weight: 700; border-radius: var(--radius-sm); padding: 2px 6px; display: inline-block; min-width: 28px; }
.score-1, .score-2 { background: var(--color-error-bg); color: var(--color-error); }
.score-3 { background: var(--color-warning-bg); color: var(--color-warning); }
.score-4, .score-5 { background: var(--color-success-bg); color: var(--color-success); }

/* ===== Footer ===== */
.app-footer {
  text-align: center;
  padding: var(--space-6) var(--space-4);
  font-size: 0.8125rem;
  color: var(--color-text-muted);
  border-top: 1px solid var(--color-border);
  margin-top: var(--space-8);
}

/* ===== Responsive ===== */
@media (max-width: 640px) {
  .container { padding: var(--space-4) var(--space-3); }
  .card { padding: var(--space-4); }
  .header-inner { padding: var(--space-2) var(--space-3); }
  .brand-title { font-size: 1rem; }
  .brand-sub { font-size: 0.6875rem; }
  .stats-grid { grid-template-columns: 1fr 1fr; }
  .eval-grid { grid-template-columns: 1fr; }
  .data-table { font-size: 0.75rem; }
  .data-table th, .data-table td { padding: var(--space-1) var(--space-2); }
}
